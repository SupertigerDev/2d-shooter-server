
export interface Hero { size: number; maxHealth: number; walkSpeed: number; gunDamage: number; }
export const HERO_PROPERTIES = {
  soldier: {
    size: 50,
    maxHealth: 200,
    walkSpeed: 0.5,
    gunDamage: 5,
  }
}

